import { RoutePlaceholder } from "@/components/navigation/route-placeholder";

export default function ParentMessagesPage() {
  return (
    <RoutePlaceholder
      eyebrow="Parent area"
      title="Messages"
      description="Parent communication with the centre and tutors will live here."
    />
  );
}
