import { RoutePlaceholder } from "@/components/navigation/route-placeholder";

export default function ParentBookingsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Parent area"
      title="Bookings"
      description="Parents will book and manage lessons from this route."
    />
  );
}
