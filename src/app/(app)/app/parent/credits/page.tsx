import { RoutePlaceholder } from "@/components/navigation/route-placeholder";

export default function ParentCreditsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Parent area"
      title="Credits"
      description="Credit balances, usage, and top-up workflows will live here."
    />
  );
}
