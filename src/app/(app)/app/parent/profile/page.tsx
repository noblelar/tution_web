import { RoutePlaceholder } from "@/components/navigation/route-placeholder";

export default function ParentProfilePage() {
  return (
    <RoutePlaceholder
      eyebrow="Parent area"
      title="Profile"
      description="Parent profile, contact details, and account preferences will live here."
    />
  );
}
