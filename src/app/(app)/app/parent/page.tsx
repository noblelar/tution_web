import { RoutePlaceholder } from "@/components/navigation/route-placeholder";

export default function ParentDashboardPage() {
  return (
    <RoutePlaceholder
      eyebrow="Parent area"
      title="Parent dashboard"
      description="Parents will see child activity, bookings, credits, reports, and messages from this dashboard."
    />
  );
}
