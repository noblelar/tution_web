import { RoutePlaceholder } from "@/components/navigation/route-placeholder";

export default function ParentChildrenPage() {
  return (
    <RoutePlaceholder
      eyebrow="Parent area"
      title="Children"
      description="Parents and guardians will manage linked students from this page."
    />
  );
}
