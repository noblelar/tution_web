import { RoutePlaceholder } from "@/components/navigation/route-placeholder";

export default function ParentChildDetailPage() {
  return (
    <RoutePlaceholder
      eyebrow="Parent area"
      title="Child profile"
      description="This page will show a parent's view of a linked student's tuition activity."
    />
  );
}
