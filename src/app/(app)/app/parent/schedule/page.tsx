import { RoutePlaceholder } from "@/components/navigation/route-placeholder";

export default function ParentSchedulePage() {
  return (
    <RoutePlaceholder
      eyebrow="Parent area"
      title="Schedule"
      description="Parent-facing lesson schedules and calendar views will live here."
    />
  );
}
